const http = require('http');
console.log(123213);
http.createServer((req, res) => {
    res.writeHead(200, {'Content-Type': 'text/plain'});
    res.end('Hello World\n');
    console.log(req.headers.host);
}).listen(Math.round((1 + Math.random()) * 1000), 'localhost');