import {existsSync, readFileSync, realpathSync} from 'fs';
import {join, resolve} from 'path';

export default (cwd) => {
    const directory = realpathSync(cwd);
    return (relatedPath = '') => {
        return resolve(directory, relatedPath);
    }  
}